apiVersion: v1
kind: Secret
metadata:
  name: openfga-postgres-credentials
type: Opaque
stringData:
  connection-string: "postgres://postgres:postgres@postgres:5432/openfga?sslmode=disable"
  # This is a placeholder - in production, use a proper secret management solution
